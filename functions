def print calendar():
import datetime

  import calendar
  def print_calendar():
  year = datetime.datetime.now()
  print(calendar.calendar(year.year))
  
  
  def delete_event():
    events  = [1, 2]
    del event[:]
    
    
  def show_events(evebts):
      print (events)
